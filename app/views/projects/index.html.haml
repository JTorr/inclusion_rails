%h1 My Projects
%h4=link_to "Add New Project", projects_new_path
%br
%h3 Current Projects


-if @projects
  -@projects.each do |project|
    %table.table.table-striped
      %tbody
        %tr
          %td=link_to "#{project.title.capitalize}", projects_show_path
        %tr

-# =link_to "#{project.name.capitalize}", projects_show_path

.row
  - if @projects
    %br
    = render partial: "project_search"
